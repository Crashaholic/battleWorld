#ifndef MOVESDB
#define MOVESDB
#pragma once
#include <iostream>
#include <string>
#include "main.h"
#endif

int damage(int attack, int opponentArmor);
bool mpChecker(int req);
void getMoveIndex(int index);
